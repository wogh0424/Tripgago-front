module.exports = {
    extends: ['react-app'], // create-react-app의 기본 설정을 상속
    rules: {
        // 여기에 추가 규칙을 정의
    },
    globals: {
        google: 'readonly',
        i18next: 'readonly'
    }
};
